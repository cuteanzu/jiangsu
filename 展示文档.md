# 江苏高校生活指北 展示文档

## 项目定位

本项目从单一的“江苏高校地图”升级为“江苏高校生活指北”。核心目标不是只展示地图，而是把江苏高校的城市分布、学校档案、校园生活、学生经验和个人作品展示串成一个有探索感的网站。

当前主线：

```text
登录页 → 沉浸式首页 → 江苏高校地图 → 城市高校列表 → 学校预览 → 学校展览详情
```

## 更新记录

### 2026-05-24 V3「探索地图册」体验升级

本次改动是项目从"高校信息查询门户"到"东方幻想×校园手账×数字地图"的定位重塑，覆盖首页简化、地图页重构、莉雅场景化、登录过渡动画和全局暖色调统一。

改动内容：

- 首页彻底简化：移除功能卡片矩阵、三段叙事 StoryRail、场景指示器、云朵漂移、大花瓣、Bokeh、Komorebi 等装饰层。
- 首页新增 HeroMapStage：直接渲染江苏 13 城市 SVG 地图（来自 jiangsu-paths.json），城市路径可 hover 浮起发光、点击跳转地图页对应城市。
- 首页色彩改为暖羊皮纸色系（#fdf7f2 基底），文案聚焦"探索江苏，遇见你的大学"。
- 首页搜索简化为仅城市搜索，保留 6 个热门城市胶囊 + 中央 CTA 按钮。
- 首页 sakura 粒子强度降至极低（maxParticles: 12, intensity: minimal）。
- 地图页省份视图移除全部 78 个学校标记点，仅显示 13 个城市标签和高校数量。
- 地图页新增城市进入平滑过渡（CSS transition）、CityPanel 右侧滑入。
- 学校预览从浮窗改为 SchoolArchiveCard（校园档案卡）：包含名称、层级标签、城市、建校年份、关键词、推荐理由、查看详情按钮。
- 学校详情视图简化，展览卡精简为三列。
- 全局色彩统一为暖手账色调：
  - 985: #c44545、211: #7b5ea7、双一流: #4a8eb5、本科: #5a9e7c
  - 文字主色 #3a2f28、强调色 #c76b5e、卡片背景 rgba(255,252,247,0.85)
- 莉雅新增 scene 场景化导游系统：home / map-province / map-city / map-school / search 五种场景对应不同提示语矩阵。
- 登录页新增 4 阶段过渡动画：小精灵聚拢 → 光点扩散 → 江苏轮廓浮现 → 进入首页。
- 移除 Works/About/Blog 路由（页面文件保留但不再可访问），消除占位导航。
- 动画原则统一：仅用 opacity+translateY、禁用 blur 入场、禁用 box-shadow 脉冲、hover 仅用 translateY(-2px)。

涉及文件：

- `src/App.tsx`（移除 3 个路由）
- `src/pages/Home.tsx`（2029 行 → 260 行，完全重写）
- `src/pages/JiangsuMap.tsx`（修改约 600 行：颜色、ArchiveCard、DeskPet、过渡）
- `src/pages/Login.tsx`（新增 80 行过渡动画）
- `src/DeskPet.tsx`（新增 40 行场景系统）
- `展示文档.md`

展示话术：

```text
这一版是项目的定位重塑。首页从带功能卡片的入口大厅，变成一张会呼吸的江苏高校地图——13 个城市分区直接可点，搜索和城市快捷入口保留，其余装饰全部削掉。
地图页不再默认散落 78 个学校标记，而是让用户从城市出发，点击后平滑放大，右侧滑入高校列表。点击学校会出现校园档案卡，像一个手账卡片而不是数据面板。
莉雅现在有场景感知能力——首页、省份、城市、学校各有不同的导览提示语。
登录页增加了聚拢→扩散→江苏轮廓浮现的过渡动画，让进入首页有仪式感。
整体色彩从亮粉亮蓝改为暖羊皮纸色系，视觉更克制、更像地图册而不是数据大屏。
```

演示路径：

1. 打开 `/login`，点击「游客体验」，观察过渡动画（聚拢光点 → 江苏轮廓 → 进入首页）。
2. 首页：确认只有标题 + 江苏 SVG 地图 + 搜索框 + 城市胶囊 + CTA 按钮 + 莉雅。
3. 悬停地图城市 → 城市浮起变色。
4. 点击南京 → bloom 转场 → 进入 `/jiangsu?city=南京市`。
5. 省份视图：确认 13 个城市标签 + 数量，无学校标记点。
6. 点击城市 → 右侧面板滑入，显示城市高校列表。
7. 点击学校 → 校园档案卡从右侧滑入。
8. 点击「查看校园详情」→ 进入详情视图。
9. 莉雅气泡随场景切换（首页 / 省份 / 城市 / 学校）。
10. `npm run lint` 和 `npm run build` 通过。

### 2026-05-22 首页门户升级

本次改动先落地“全面大升级”的第一步：把 `/home` 从临时导航页改成正式门户首页。

改动内容：

- 首页标题从普通入口页升级为“江苏高校生活指北”的产品入口。
- 增加“探索江苏，遇见你的大学”的主视觉文案，明确网站价值。
- 保留樱花、柔光、景深、莉雅等原有氛围元素，让首页和登录页保持统一。
- 新增搜索入口，可以搜索城市或高校，并跳转到对应 `/jiangsu` 地图状态。
- 新增热门城市快捷入口：南京、苏州、徐州、无锡、南通、常州。
- 新增四个功能入口：江苏高校地图、高校生活指北、经验分享、作品档案。
- “高校生活指北”和“经验分享”先作为规划中入口展示，不开放点击。
- 新增数据胶囊：江苏城市入口、已录入高校、热门高校起点。

涉及文件：

- `src/pages/Home.tsx`
- `展示文档.md`

### 2026-05-22 登录页小精灵交互稳定化

本次改动处理登录页点击小精灵时的闪烁和不稳定问题，并让四个小精灵真正承担网站能力导览。

问题原因：

- 原登录页的小精灵同时使用内联 `transform` 和 CSS keyframes 修改 `transform`。
- 浏览器会在两套 transform 之间抢控制权，点击、快速移动鼠标或表单交互时容易出现跳动、闪烁。
- 原来的卡片点击会触发全局弹跳状态，导致点击输入框、小精灵、表单区域时都可能影响角色动画。

改动内容：

- 移除小精灵本体上的随机摇晃和点击弹跳动画。
- 小精灵本体只保留一套稳定的内联 transform，用于眼睛跟随、轻微呼吸和输入反馈。
- 点击小精灵后改为切换“能力说明卡”和发光高亮，不再改变角色布局。
- 四个小精灵分别绑定网站能力：
  - 紫色：地图探索
  - 黑色：高校档案
  - 橙色：经验分享
  - 黄色：生活指北
- 登录页文案改为中文，和“江苏高校生活指北”的项目定位统一。
- 登录按钮改为“进入校园入口”，游客体验按钮可直接进入首页。

涉及文件：

- `src/pages/Login.tsx`
- `展示文档.md`

展示话术：

```text
登录页不只是账号入口，而是网站世界观的第一屏。
四个小精灵现在分别代表地图探索、高校档案、经验分享和生活指北。
点击它们不会再触发不稳定动画，而是切换左侧说明卡，让用户在登录前就理解网站的核心能力。
```

### 2026-05-22 首页动效系统 V1

本次改动开始落地“重做动效系统”的第一版，不引入新依赖，先用 CSS keyframes + React state 建立统一交互反馈。

改动内容：

- 首页顶部、主标题、按钮、右侧面板、功能卡片、统计胶囊增加分段入场动画。
- 主按钮增加扫光、箭头位移、点击缩放和跳转光晕。
- 功能卡片增加 hover 上浮、边框发光、图标旋转、箭头推进和点击压感。
- 搜索框增加聚焦高亮和建议下拉。
- 搜索建议支持城市和高校两类：
  - 城市建议会进入对应城市地图。
  - 高校建议会进入对应城市并选中学校。
- 城市快捷入口增加 hover 推荐语。
- 莉雅在首页增加固定引导气泡，会根据搜索、城市悬停、功能卡片 hover 切换提示文案。
- “规划中”状态改为更产品化的“持续补充中 / 等待同学投稿”。
- 修复 `Map` 图标和 JavaScript `Map` 类型重名导致的 TypeScript 构建问题。

涉及文件：

- `src/pages/Home.tsx`
- `展示文档.md`

展示话术：

```text
首页现在不再是静态入口，而是一个会回应用户操作的校园入口大厅。
用户进入页面时，内容分段浮入；悬停功能卡片时，卡片会产生上浮、发光和箭头推进；
搜索学校或城市时，莉雅会给出建议，搜索框会出现可点击的城市和高校结果。
点击主入口时，页面会出现粉蓝光晕转场，再进入江苏高校地图。
```

演示路径：

1. 打开 `/home`，展示首页内容分段入场。
2. 悬停“开始探索地图”和四个功能卡片，展示统一 hover 反馈。
3. 在搜索框输入“南京”或“南京大学”，展示建议下拉。
4. 悬停城市快捷入口，展示莉雅推荐语变化。
5. 点击主入口，展示光晕转场进入 `/jiangsu`。

### 2026-05-22 首页性能与电影感布局修复

本次改动针对首页动效 V1 暴露出的卡顿、闪屏、莉雅气泡不同步、右侧卡片被裁切问题做稳定化。

问题原因：

- 首页鼠标视差使用 React state，每次鼠标移动都会触发整页重新渲染，导致卡顿。
- 入场动画使用 `filter: blur(...)`，在大面积毛玻璃和背景图叠加时容易造成闪屏。
- 莉雅气泡是首页里的固定定位元素，而莉雅本体是可拖拽组件，两者不是同一个坐标系统。
- 右侧功能卡片没有独立滚动容器，视口高度不足时底部卡片和统计区域会被裁掉。

改动内容：

- 首页鼠标视差改为直接写 CSS 变量，不再触发 React 重渲染。
- 入场动画移除 blur，只保留透明度和位移动画，降低 GPU 压力。
- 首页樱花粒子强度降为更克制的展示级别，避免和其它背景动效叠加过重。
- 右侧面板改为独立可滚动舞台，支持滑动查看更多功能卡片和统计信息。
- 右侧滚动区域加入顶部/底部渐隐遮罩，整体更像电影式信息面板。
- 莉雅引导气泡移动到 `DeskPet` 内部，由莉雅本体统一渲染，气泡会跟随她。
- 首页禁用莉雅自动跟随鼠标，避免和用户阅读、卡片 hover 争抢注意力。
- 页面遮罩加入更轻的上下暗角，增强电影感并压住背景。

涉及文件：

- `src/pages/Home.tsx`
- `src/DeskPet.tsx`
- `展示文档.md`

展示话术：

```text
这一版不是继续堆动画，而是把动效系统稳定下来。
首页鼠标视差现在不再触发整页重渲染，莉雅的提示气泡也跟随本体移动。
右侧功能区变成可以滑动的信息舞台，底部内容不会再被裁掉。
整体加了更克制的上下暗角和滚动渐隐，让首页更接近电影式门户。
```

验证路径：

1. 打开 `/home`，快速移动鼠标，确认页面不再明显卡顿。
2. 观察页面进入动画，确认没有大面积闪白或闪屏。
3. 悬停城市、卡片、搜索框，确认莉雅提示跟着莉雅本体显示。
4. 在右侧功能区滚动，确认底部卡片和统计信息可以完整查看。
5. 拖动莉雅，确认气泡仍然跟随她。

### 2026-05-22 江苏 SVG 地图管线第一阶段

本次改动开始落地 `地图.md` 里的地图制作路线：先建立 GeoJSON 到 SVG path 的工程管线，再把江苏轮廓接入首页展示叙事。

改动内容：

- 新增 `npm run build:map` 命令。
- 新增 `scripts/build-jiangsu-svg.mjs`，读取 `public/jiangsu-geo.json` 并生成城市 path 数据。
- 生成 `src/assets/map/jiangsu-paths.json`，当前已确认 13 个城市全部生成。
- 脚本没有直接使用 `geoMercator().fitExtent(...)`，而是复用项目里的手动投影适配思路，避免 GeoJSON 环方向导致地图被压缩到中心。
- 生成文件移除时间戳，保证 `npm run build:map` 输出稳定，避免每次运行都产生无意义改动。
- 将根目录 `jiangsu.svg` 作为省级轮廓资产接入到 `src/assets/map/jiangsu-outline.svg`。
- 删除旧的 `jiangsu-3d.html` 实验残留，避免 Vite 扫描已经移除的 Three.js 依赖。
- 首页 `/home` 从单屏门户升级为可向下滚动的电影式展示页。
- 首页新增“江苏 SVG 地图制作线”第二屏，展示江苏轮廓、城市节点、数据管线和进入地图按钮。
- 新增 `JiangsuDataMapPreview`，直接读取 `jiangsu-paths.json` 渲染 13 城市 SVG path，第二屏不再只是单张省轮廓图片。
- `JiangsuDataMapPreview` 采用懒加载，地图 path 数据被拆成独立 chunk，避免拖大首页首屏包体积。
- 页面主体滚动改为 `Wrapper` 内部滚动，兼容当前全局 `body` 禁止滚动的设置。

涉及文件：

- `package.json`
- `scripts/build-jiangsu-svg.mjs`
- `tsconfig.app.json`
- `src/components/JiangsuDataMapPreview.tsx`
- `src/assets/map/jiangsu-outline.svg`
- `src/assets/map/jiangsu-paths.json`
- `src/pages/Home.tsx`
- `jiangsu-3d.html`
- `展示文档.md`

展示话术：

```text
地图不再只靠一张生成图撑视觉，而是开始建立真实数据管线。
江苏 GeoJSON 会被构建脚本转换成 13 个城市的独立 SVG path，后续 hover、点击下钻、城市高光和滚动描边都可以复用这份数据。
首页第二屏已经开始直接渲染这份城市 path 数据，并且做了懒加载拆包，让作品从“好看”过渡到“有工程底座”。
```

验证路径：

1. 运行 `npm run build:map`，确认输出 13 个城市。
2. 打开 `/home`，首屏仍是门户首页。
3. 向下滚动，查看“江苏 SVG 地图制作线”第二屏，确认能看到 13 城市 SVG 数据地图。
4. 悬停 / 点击第二屏城市 path 或城市节点，确认仍能跳转到 `/jiangsu` 对应状态。
5. 运行 `npm run lint` 和 `npm run build`。

### 2026-05-22 首页地图叙事第二屏视觉修正

本次改动修正“江苏 SVG 地图制作线”第二屏观感差的问题。

问题原因：

- 原始 SVG path 属于工程数据，不适合直接放大当主视觉。
- 13 城市 path 和手写百分比城市标签不是同一个空间系统，容易出现标签漂移和视觉割裂。
- 绿色调试底板、错位标签和莉雅气泡叠加后，页面更像开发调试图，而不是正式展示页。

改动内容：

- 第二屏右侧主视觉恢复为成熟的江苏沙盘图，保证第一眼观感稳定。
- 删除第二屏手写城市标签，避免和真实 path 坐标系统冲突。
- `JiangsuDataMapPreview` 改为右下角“小型数据验证面板”，只承担技术证明和轻交互。
- SVG path 仍然保留 hover / click 能力，但不再抢占视觉中心。
- 页面明确采用“双层策略”：
  - 视觉层：生成沙盘图负责展示质感。
  - 数据层：SVG path 负责后续 hover、click、命中区域和下钻逻辑。
- 保持懒加载拆包，首页主包不被地图 path 数据拖大。

涉及文件：

- `src/pages/Home.tsx`
- `src/components/JiangsuDataMapPreview.tsx`
- `展示文档.md`

展示话术：

```text
这一版修正了一个关键设计判断：原始 SVG path 是数据底座，不是最终视觉。
正式展示仍然用沙盘图承接视觉冲击力；SVG path 被收进数据验证面板，证明 13 城市已经可以独立交互。
后续进入地图页时，会采用“沙盘图做视觉层，SVG path 做交互层”的方式推进，而不是把调试地图直接放大给用户看。
```

验证路径：

1. 打开 `/home` 并滚动到第二屏。
2. 确认右侧主视觉是沙盘图，不再是绿色调试底板。
3. 确认右下角有 SVG path 数据验证面板。
4. 悬停 / 点击验证面板里的城市 path，确认可以跳转到对应城市地图。
5. 运行 `npm run lint` 和 `npm run build`。

补充修复：

- 确认 `/jiangsu/province-stage.png` 可以直接访问后，将第二屏沙盘图从 CSS 多背景改为明确的 `<img>` 图层。
- 这样可以避免渐变背景、伪元素遮罩或多背景解析导致主视觉不显示。

## 当前展示话术

可以这样介绍首页：

```text
这是江苏高校生活指北的入口页。它不是传统后台菜单，而是一个校园探索大厅。
用户可以直接进入江苏高校地图，也可以搜索城市或学校；后续还会扩展宿舍、食堂、交通、学习氛围和学生经验分享。
右侧的莉雅建议和功能入口会承担导览作用，让网站从“作品展示”升级成“可持续扩展的平台”。
```

## 当前演示路径

1. 打开 `/home`，展示首页整体氛围和“生活指北”定位。
2. 点击“开始探索地图”，进入 `/jiangsu`。
3. 回到 `/home`，搜索“南京大学”，跳转到南京大学对应地图状态。
4. 点击“先看南京高校”，进入南京市高校页面。
5. 展示“生活指北”和“经验分享”作为后续规划入口。

## 后续升级顺序

第一阶段：完成门户首页。

- 首页主视觉、搜索、城市入口、功能入口。
- 明确项目定位和展示文档记录规则。

第二阶段：优化登录页。

- 强化登录进入仪式感。
- 让四个小精灵对应网站能力。
- 登录成功后加入更顺滑的转场。

第三阶段：稳定地图页体验。

- 保留当前沙盘图方案，不再继续投入实验 Three.js 3D。
- 打磨省级、市级、学校预览、学校详情的排版。
- 提升移动端和窄屏适配。

第四阶段：补内容价值。

- 新增高校生活指北模块。
- 增加宿舍、食堂、交通、学习氛围、新生建议等字段。
- 后续支持经验分享和投稿。

## 以后每次改动的记录规则

每次代码改动后，都在本文档追加四部分：

```text
1. 改动时间
2. 改动内容
3. 涉及文件
4. 展示话术 / 演示路径
```

这样后续做答辩、路演、作品集整理时，可以直接从文档里提炼展示逻辑。

### 2026-05-24 首页 V2 系统性升级

本次改动是首页从"静态入口"到"沉浸式校园门户"的完整升级，覆盖视觉层级、滚动动画、点击反馈、叙事区域、背景氛围和交互打磨六个维度。

改动内容：

**视觉层级重构：**
- 将 PortalGrid 从 2x2 等分网格改为 主卡片全宽 + 三列次卡片 的非对称布局。
- 新增 PrimaryPortalCard 组件：继承 PortalCard，更大高度（190px）、更大字号（22px）、带脉冲呼吸光晕动画（3.8s 周期），强化"探索地图"的主入口地位。
- 禁用卡片（高校生活指北/经验分享）右上角添加"暑期上线"小徽章。
- Hero 副标题、GuidePanel 莉雅介绍、城市悬停提示等文案全面升级为更口语化、更有邀请感的表达。

**滚动触发动画：**
- 新建 `src/hooks/useInView.ts`：基于 IntersectionObserver，支持 triggerOnce、threshold、rootMargin 配置。
- StoryScene 改为 `$visible` 驱动的 opacity+translate 过渡，内容在滚动进入视口时才播放入场动画。
- StoryCopy 子元素使用 `$visible` + inline `transitionDelay` 做 stagger 延迟，营造逐层展开的电影感。
- StatsStrip 内 StatPill 做逐个延迟入场。

**卡片点击增强：**
- 新增 cardClickRipple 关键帧：点击卡片时从中心扩散粉色光圈 + 轻微 scale 0.96→1 回弹。
- PortalCard 新增 `$clicking` prop，点击时触发 440ms 波纹动画。
- navigateWithBloom 增强：接收 cardId 参数，卡片波纹（440ms）与路由光晕（230ms 触发）双层叠加 ~170ms，营造"按下→回应→跳转"的连贯体验。

**叙事区域三段式升级（StoryRail V2）：**
- 场景 1 "探索方式"：从地图出发，看见每一所大学。保留 SVG 数据验证面板。Steps 改为用户视角。
- 场景 2 "校园档案"（新增）：布局反转（视觉左、文案右），展示 985/211/双一流/本科层级徽章。强调数据+体验结合。
- 场景 3 "校园向导"（新增）：莉雅四形态展示（calm/happy/archery/dance），介绍拖拽、建议、推荐功能。
- 新增 SceneIndicator 圆点指示器 + SceneConnector 渐变连接线，随滚动自动切换激活场景。
- Shell 底部新增 ScrollHint（弹跳箭头），滚动超过 100px 后自动淡出。

**背景氛围升级：**
- 新增 AnimatedBG 层：28s 缓慢缩放呼吸动画（scale 1→1.025→1），配合鼠标视差。
- 新增 3 个 CloudShape：38vw 模糊渐变椭圆，48-62s 横跨屏幕漂移。
- 新增 4 个 LargeDriftPetal：80-140px 模糊花瓣，28-42s 跨视口漂移+旋转。
- 完全尊重 prefers-reduced-motion：检测媒体查询，减少动效时自动隐藏/冻结云层、花瓣和背景呼吸。
- Z-index 层叠重组：AnimatedBG(0) → Clouds/FX/Bokeh(1) → SoftWash(2) → Petals(3) → Shell(8)。

**UX 打磨：**
- 搜索建议完整键盘导航：↑↓ 键选择、Enter 确认、Escape 关闭，activeSuggestionIndex 高亮当前项。
- 新增 MobileNavFab：640px 以下底部浮动菜单按钮，点击展开 3 个导航选项。
- StoryScene 移动端 min-height 降至 300px，修复小屏布局。
- StatPill 标签改为体验导向（"探索城市""收录高校""推荐起点"）。
- 交互元素 hover 响应升级：Title 增加 hover 阴影变化、StageImageLayer 增加 fade-in 过渡。

涉及文件：
- `src/pages/Home.tsx`（主要改动，~300 行新增/修改）
- `src/hooks/useInView.ts`（新建）
- `展示文档.md`

展示话术：

```text
首页 V2 从视觉层级、动效系统和内容叙事三个维度重新设计。
"探索地图"现在是全宽主入口，带脉冲呼吸光晕；次卡片被收进三列网格，禁用卡片标注"暑期上线"。
用户向下滚动时，三个场景依次从视口浮入——场景指示器会实时跟踪阅读进度。
背景不再是静态图片：云状渐变在漂移，大型花瓣在旋转，底图在缓慢呼吸——所有这些在检测到减少动效偏好时会自动冻结。
搜索框现在完全支持键盘操作，移动端有浮动菜单。
点击任何卡片都会先触发卡片波纹，再叠加路由光晕，形成"按下→回应→跳转"的连贯手感。
```

演示路径：

1. 打开 `/home`，观察首屏：主卡片全宽、脉冲光晕、次卡片三列网格。
2. 搜索框输入"南京"，用 ↑↓ 键选择建议，Enter 确认跳转。
3. 悬停城市胶囊，观察莉雅提示变化。
4. 向下滚动：三个场景依次入场，场景指示器圆点同步切换。
5. 观察背景：等待 10-15 秒，注意云状漂移和大花瓣动画。
6. 点击主卡片：卡片波纹 → 路由光晕 → 跳转 /jiangsu。
7. 缩小窗口至 375px，确认移动端 FAB 导航可用。
8. 运行 `npm run lint` 和 `npm run build` 通过。
