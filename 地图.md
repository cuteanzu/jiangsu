可以，你这个“准备江苏 SVG 地图”建议不要手画，而是走一条标准工程路线：

> **江苏 GeoJSON 数据 → 简化压缩 → 转成 SVG path → 封装成 Vue 组件 → 绑定 hover / click / GSAP 动效**

这样后期你做省级、市级、高校下钻都能复用。

---

# 1. 先明确你要的地图类型

你现在需要的不是普通图片，而是这种结构：

```html
<svg viewBox="0 0 1000 900">
  <path data-city="南京市" d="M..." />
  <path data-city="苏州市" d="M..." />
  <path data-city="无锡市" d="M..." />
</svg>
```

每个城市都是一个独立 `path`，这样你才能做到：

```text
hover 城市发光
click 城市下钻
城市区域浮起
按高校数量改变颜色
滚动时路径描边
点击后地图放大
```

---

# 2. 获取江苏 GeoJSON 数据

推荐从 **DataV.GeoAtlas** 获取行政区 GeoJSON。阿里云文档说明，GeoJSON 是一种地理空间信息数据交换格式，也可以通过 DataV.GeoAtlas 获取或编辑 GeoJSON 地理数据。([阿里云][1])

江苏省行政区划代码是：

```text
320000
```

你可以尝试下载江苏省含子区域 GeoJSON：

```bash
mkdir -p src/assets/map

curl -L "https://geo.datav.aliyun.com/areas_v3/bound/320000_full.json" \
  -o src/assets/map/jiangsu.geojson
```

说明：

```text
320000.json       通常是江苏省整体边界
320000_full.json  通常是江苏省 + 下级城市边界
```

你要做城市可点击，所以优先用：

```text
320000_full.json
```

如果这个地址后续不可用，就直接去 DataV.GeoAtlas 页面手动选择“江苏省”，然后下载带子区域的 GeoJSON。DataV.GeoAtlas 是阿里云 DataV 提供的地理小工具。([阿里云数据帝][2])

---

# 3. 安装转换工具

你的项目里安装：

```bash
npm install d3-geo fs-extra
npm install -D mapshaper
```

其中：

```text
d3-geo：把经纬度 GeoJSON 投影成 SVG path
mapshaper：压缩、简化、清理 GeoJSON
```

Mapshaper 支持 GeoJSON、TopoJSON、Shapefile 等格式，并且可以做简化、裁剪、清理等地图处理。([Mapshaper][3])

---

# 4. 简化 GeoJSON

原始 GeoJSON 点太多，直接放进网页会卡。先压缩：

```bash
npx mapshaper src/assets/map/jiangsu.geojson \
  -simplify 20% keep-shapes \
  -o precision=0.00001 format=geojson \
  src/assets/map/jiangsu.simple.geojson
```

解释：

```text
-simplify 20%       保留大约 20% 的形状点
keep-shapes         防止小区域被简化消失
precision=0.00001   降低坐标精度，减少文件体积
format=geojson      输出 GeoJSON
```

Mapshaper 官方命令中就支持 `-simplify` 和输出精度控制，适合网页地图预处理。([Mapshaper][3])

如果你觉得地图边缘太粗糙，就改成：

```bash
-simplify 40%
```

如果你想极致性能，就改成：

```bash
-simplify 10%
```

我建议你的项目先用：

```text
20% - 35%
```

视觉和性能比较平衡。

---

# 5. 写一个脚本把 GeoJSON 转成 SVG path

新建：

```text
scripts/build-jiangsu-svg.mjs
```

代码如下：

```js
import fs from "fs-extra"
import { geoMercator, geoPath } from "d3-geo"

const input = "src/assets/map/jiangsu.simple.geojson"
const output = "src/assets/map/jiangsu-paths.json"

const width = 1000
const height = 900

const geojson = await fs.readJson(input)

const projection = geoMercator().fitSize([width, height], geojson)
const pathGenerator = geoPath(projection)

const cities = geojson.features.map((feature) => {
  const props = feature.properties || {}

  const name =
    props.name ||
    props.fullname ||
    props.NAME ||
    props.adcode ||
    "未知城市"

  const adcode = props.adcode || props.adcode_pro || props.id || ""

  const d = pathGenerator(feature)
  const centroid = pathGenerator.centroid(feature)

  return {
    name,
    adcode,
    d,
    centroid: {
      x: Number(centroid[0].toFixed(2)),
      y: Number(centroid[1].toFixed(2))
    },
    rawProperties: props
  }
})

await fs.outputJson(
  output,
  {
    viewBox: `0 0 ${width} ${height}`,
    cities
  },
  {
    spaces: 2
  }
)

console.log(`生成完成：${output}`)
console.log(`城市数量：${cities.length}`)
```

D3 的 `projection.fitSize([width, height], object)` 可以把 GeoJSON 自动缩放并平移到指定画布尺寸内，非常适合你这种把行政区转 SVG 的场景。([D3.js][4])

运行：

```bash
node scripts/build-jiangsu-svg.mjs
```

生成：

```text
src/assets/map/jiangsu-paths.json
```

大概结构会是：

```json
{
  "viewBox": "0 0 1000 900",
  "cities": [
    {
      "name": "南京市",
      "adcode": 320100,
      "d": "M421.2,318.4L...",
      "centroid": {
        "x": 512.31,
        "y": 388.12
      }
    }
  ]
}
```

---

# 6. 封装成 Vue 组件

新建：

```text
src/components/JiangsuSvgMap.vue
```

```vue
<template>
  <div class="jiangsu-map-wrap">
    <svg
      class="jiangsu-map"
      :viewBox="mapData.viewBox"
      preserveAspectRatio="xMidYMid meet"
    >
      <defs>
        <filter id="cityGlow" x="-30%" y="-30%" width="160%" height="160%">
          <feGaussianBlur stdDeviation="4" result="blur" />
          <feMerge>
            <feMergeNode in="blur" />
            <feMergeNode in="SourceGraphic" />
          </feMerge>
        </filter>

        <linearGradient id="cityGradient" x1="0" y1="0" x2="1" y2="1">
          <stop offset="0%" stop-color="#1d4ed8" />
          <stop offset="100%" stop-color="#38bdf8" />
        </linearGradient>
      </defs>

      <g class="map-layer">
        <path
          v-for="city in mapData.cities"
          :key="city.adcode || city.name"
          class="city-path"
          :class="{ active: activeCity === city.name }"
          :d="city.d"
          :data-city="city.name"
          @mouseenter="handleEnter(city)"
          @mouseleave="handleLeave"
          @click="handleClick(city)"
        />
      </g>

      <g class="city-label-layer">
        <g
          v-for="city in mapData.cities"
          :key="`${city.name}-label`"
          class="city-label"
          :transform="`translate(${city.centroid.x}, ${city.centroid.y})`"
        >
          <circle r="4" />
          <text y="-10">{{ city.name.replace('市', '') }}</text>
        </g>
      </g>
    </svg>
  </div>
</template>

<script setup>
import { ref } from "vue"
import mapData from "@/assets/map/jiangsu-paths.json"

const emit = defineEmits(["select-city", "hover-city"])

const activeCity = ref("")

function handleEnter(city) {
  activeCity.value = city.name
  emit("hover-city", city)
}

function handleLeave() {
  activeCity.value = ""
  emit("hover-city", null)
}

function handleClick(city) {
  emit("select-city", city)
}
</script>

<style scoped>
.jiangsu-map-wrap {
  width: 100%;
  height: 100%;
  position: relative;
}

.jiangsu-map {
  width: 100%;
  height: 100%;
  overflow: visible;
}

.city-path {
  fill: rgba(30, 64, 175, 0.45);
  stroke: rgba(147, 197, 253, 0.8);
  stroke-width: 1.2;
  cursor: pointer;
  transition:
    fill 0.25s ease,
    transform 0.25s ease,
    filter 0.25s ease,
    stroke 0.25s ease;
  transform-box: fill-box;4
  transform-origin: center;
}

.city-path:hover,
.city-path.active {
  fill: url("#cityGradient");
  stroke: #e0f2fe;
  filter: url("#cityGlow");
  transform: translateY(-4px) scale(1.015);
}

.city-label {
  pointer-events: none;
}

.city-label circle {
  fill: #e0f2fe;
  filter: drop-shadow(0 0 8px rgba(56, 189, 248, 0.9));
}

.city-label text {
  fill: #e0f2fe;
  font-size: 18px;
  font-weight: 600;
  text-anchor: middle;
  paint-order: stroke;
  stroke: rgba(15, 23, 42, 0.9);
  stroke-width: 3px;
}
</style>
```

---

# 7. 在省级页面使用

```vue
<template>
  <section class="province-page">
    <div class="map-stage">
      <JiangsuSvgMap
        @select-city="goCity"
        @hover-city="setHoverCity"
      />
    </div>

    <aside class="info-panel">
      <h2>{{ hoverCity?.name || "江苏高校总览" }}</h2>
      <p v-if="hoverCity">
        点击进入 {{ hoverCity.name }} 高校分布页面
      </p>
      <p v-else>
        选择一座城市，查看高校分布与学校详情。
      </p>
    </aside>
  </section>
</template>

<script setup>
import { ref } from "vue"
import { useRouter } from "vue-router"
import JiangsuSvgMap from "@/components/JiangsuSvgMap.vue"

const router = useRouter()
const hoverCity = ref(null)

function setHoverCity(city) {
  hoverCity.value = city
}

function goCity(city) {
  router.push({
    name: "CityMap",
    params: {
      cityCode: city.adcode
    },
    query: {
      name: city.name
    }
  })
}
</script>
```

---

# 8. 给地图加“苹果官网式描边动画”

这个很关键。你首页滚动时可以让江苏轮廓慢慢被画出来。

安装 GSAP：

```bash
npm install gsap
```

在组件挂载后加：

```js
import { onMounted } from "vue"
import gsap from "gsap"

onMounted(() => {
  const paths = document.querySelectorAll(".city-path")

  paths.forEach((path) => {
    const length = path.getTotalLength()

    gsap.set(path, {
      strokeDasharray: length,
      strokeDashoffset: length,
      opacity: 0
    })
  })

  gsap.to(paths, {
    strokeDashoffset: 0,
    opacity: 1,
    duration: 1.8,
    stagger: 0.05,
    ease: "power2.out"
  })
})
```

效果：

```text
江苏城市边界逐条被光线画出来
然后城市区域逐渐填充
最后高校光点出现
```

这个比普通地图高级很多。

---

# 9. 高校点位怎么放？

你有两种方案。

## 方案 A：手动配置点位，最快

适合你现在先做展示。

```ts
export const schoolPoints = [
  {
    id: 1,
    name: "南京大学",
    city: "南京市",
    x: 510,
    y: 350,
    level: "985/211/双一流"
  },
  {
    id: 2,
    name: "东南大学",
    city: "南京市",
    x: 535,
    y: 370,
    level: "985/211/双一流"
  }
]
```

然后在 SVG 里面画：

```vue
<g class="school-layer">
  <circle
    v-for="school in schoolPoints"
    :key="school.id"
    class="school-dot"
    :cx="school.x"
    :cy="school.y"
    r="5"
  />
</g>
```

适合演示，效率最高。

---

## 方案 B：真实经纬度投影，后期上线用

如果你高校数据里有：

```text
lng：经度
lat：纬度
```

那就用和地图一样的 projection，把经纬度转成 SVG 坐标。

你可以在生成脚本里同时导出 projection 参数，或者直接把高校点位也放到脚本里一起处理。

示例：

```js
const [x, y] = projection([school.lng, school.lat])
```

D3 投影函数接收 `[longitude, latitude]`，返回 `[x, y]` 像素坐标。([D3.js][4])

---

# 10. 你最终文件结构应该是这样

```text
src
├── assets
│   └── map
│       ├── jiangsu.geojson
│       ├── jiangsu.simple.geojson
│       ├── jiangsu-paths.json
│       └── school-points.json
│
├── components
│   ├── JiangsuSvgMap.vue
│   ├── CityTooltip.vue
│   └── SchoolDotLayer.vue
│
├── views
│   ├── LandingScrollView.vue
│   ├── ProvinceMapView.vue
│   └── CityMapView.vue
│
└── scripts
    └── build-jiangsu-svg.mjs
```

注意：`scripts` 可以放项目根目录，不一定放 `src` 里面。

---

# 11. 常见坑

## 坑 1：下载的是整个江苏轮廓，不是城市边界

你需要的是：

```text
320000_full.json
```

不是：

```text
320000.json
```

因为你要每个城市单独点击。

---

## 坑 2：城市名称字段不统一

不同 GeoJSON 里字段可能是：

```text
name
fullname
adcode
NAME
```

所以脚本里我写了兼容：

```js
props.name || props.fullname || props.NAME || props.adcode
```

---

## 坑 3：地图太大导致首屏卡顿

解决：

```text
mapshaper 简化
输出 json 压缩
不要在 DOM 里画太多高校点
高校点按城市懒加载
```

---

## 坑 4：SVG hover 变形导致位置偏移

一定要加：

```css
transform-box: fill-box;
transform-origin: center;
```

否则城市区域缩放会很怪。

---

# 12. 我建议你现在先做这个版本

你先不要一步到位做真实经纬度高校点。第一版建议：

```text
1. 下载江苏 320000_full GeoJSON
2. mapshaper 简化
3. d3-geo 转 path
4. Vue 渲染城市 path
5. hover 发光
6. click 进入城市页
7. 手动配置几个重点高校点位
8. 加 GSAP 描边动画
```

这样最快可以把核心视觉跑起来。

---

# 13. 最终你要达到的效果

江苏 SVG 地图应该不是普通行政区地图，而是这种感觉：

```text
深色背景
江苏轮廓像光绘一样浮现
13 个城市分区微微发光
hover 城市向上浮起
点击城市后地图放大推进
城市高校点位从中心扩散
莉雅在右下角提示：
“点击南京，我带你看看高校最集中的区域！”
```

你先把这套 SVG 地图管线搭起来，后面无论是做苹果官网式滚动，还是做城市下钻、高校详情、莉雅引导，都会顺很多。

[1]: https://www.alibabacloud.com/help/zh/datav/datav-7-0/user-guide/map-data-format-1 "
 地图数据格式 - DataV数据可视化 - 阿里云

"
[2]: https://datav.aliyun.com/tools/atlas "DataV.GeoAtlas地理小工具系列"
[3]: https://mapshaper.org/docs/reference.html "Command reference · mapshaper docs"
[4]: https://d3js.org/d3-geo/projection "Projections | D3 by Observable"
