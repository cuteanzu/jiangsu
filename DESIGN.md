# 江苏校园指北设计系统

## 设计方向

产品界面采用“春日校园 + 江南纸感 + 数据工作台”。基础体验要像一个可靠工具，视觉气质保留樱花、纸张、地图、档案和校园笔记的温度。

视觉参考可以吸收 Awwwards 类作品站的“强版式、强模块身份、滚动网格、悬停反馈和非对称节奏”，但不能牺牲产品效率。所有炫技都要转译成更清晰的数据入口、更明确的状态和更好的比较体验。

青春感不是粉色滤镜。页面应该像春天傍晚的校园入口：轻、亮、有风，但数据仍然能被快速扫描。樱花粉负责情绪，新叶绿负责生活状态，春水蓝负责数据分析，暖纸色负责可信度。

樱花不是只改卡片颜色。页面必须使用真实或生成的樱花校园视觉资产、花瓣层、枝影、路径节点和有主题的可视化组件，让用户一眼知道这是“春日校园指北”，不是普通后台换皮。

## 使用场景

用户通常在白天或晚上用电脑反复查学校、筛城市、对比生活条件。界面应以浅色为主、信息密度适中、状态明确，避免让用户在复杂动画里找入口。

## 色彩

- 页面底色：`oklch(97% 0.018 78)`，带春日暖度的纸色。
- 主文本：`oklch(22% 0.035 48)`。
- 次文本：`oklch(47% 0.032 58)`。
- 边框：`oklch(84% 0.032 62 / 0.72)`。
- 樱花粉：`oklch(70% 0.12 18)`，用于主行动、选中、青春氛围。
- 新叶绿：`oklch(56% 0.10 145)`，用于生活画像和完成状态。
- 春水蓝：`oklch(56% 0.09 205)`，用于问答、状态和数据分析。
- 暖杏色：`oklch(78% 0.09 66)`，用于提醒、候选、轻量高亮。
- 警示红：`oklch(42% 0.12 32)`。

## 字体

- 产品 UI：`"Noto Sans SC", "PingFang SC", system-ui, sans-serif`。
- 品牌标题和长文标题可以使用 `"Noto Serif SC", "Songti SC", serif`。
- 数据页和表格优先使用无衬线，保持可读性。
- 产品页面不使用随视口缩放的字体。

## 布局

- 核心工具页采用顶部概览、左侧筛选、中间结果、右侧洞察的工作台结构。
- 卡片圆角不超过 8px。
- 不使用嵌套卡片，列表项、面板和表格要保持清楚边界。
- 地图页负责沉浸感，数据页负责密度，经验页负责阅读，问答页负责快速结论。

## 模块身份

- 高校库是筛选台：信息密度更高，强调筛选、比较、排序和资料完整度。
- 探索地图是发现台：强调空间关系、城市路径、学校分布和档案入口。
- 校园经验是现场笔记：强调阅读节奏、叙事细节、真实体感和长文本判断。
- 问答是分诊台：强调问题优先级、短结论、适用条件和下一步动作。
- 个人页是工作台：强调用户资产、登录收益、投稿闭环和持续维护。

## 高级感落点

- 页面第一屏要有明确的视觉命题，不做普通标题加卡片堆叠。
- 数据页面使用春日档案、候选签、地图网格、编号系统和轻量数据轨道建立识别度。
- 核心页面首屏要有真实视觉资产承载氛围，背景图不能被压到只剩色块。
- 樱花元素要进入结构：花签状态、花瓣轨道、路径节点、枝影分层，而不是只作为装饰贴纸。
- 自有素材放在 `public/jiangsu-elements/`，樱花枝和花瓣用于氛围层，江南水线用于江苏地域识别，猫元素优先用于登录、空状态、个人页和成功反馈。
- 关键操作保留标准控件，但按钮、选中态、候选池和数据条要有一致的状态语言。
- 大量数据优先做可扫描的行、轨道、分布条和对比位，不用等宽卡片网格消耗密度。

## 交互原则

- 默认状态要轻，悬停状态像“被风吹起一点”：小幅上浮、边框变清晰、背景变亮。
- 选中状态使用樱花粉或新叶绿的浅底，不使用重阴影压住内容。
- 数据可视化优先使用横向轨道、花签式指标、城市分布条和候选对比位；避免只展示“几个人、几条”的硬计数。
- 点击后的路径要明确：筛选进入候选池，候选池进入地图档案，经验进入详情阅读，问答进入结论页。

## 组件

- 搜索框：左侧搜索图标，焦点时边框和浅色外发光。
- 筛选：使用按钮组、select 和 chip，不发明奇怪控件。
- 学校列表：每行展示学校身份、城市、层次、类型、数据完整度、内容入口。
- 状态：加载用骨架或短文案，错误给出兜底数据说明。

## 禁止

- 不用渐变文字。
- 不用大面积玻璃拟态。
- 不用装饰性光球或纯装饰背景块。
- 不把所有内容做成同尺寸卡片网格。
- 不让问答和校园经验承担同一种信息任务。
