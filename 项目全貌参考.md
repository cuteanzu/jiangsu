# 江苏高校生活指北 — 项目全貌参考

> 生成日期：2026-05-24 | 基于当前代码实际状态

---

## 一、技术栈

| 层 | 技术 |
|---|---|
| 框架 | React 19 + TypeScript 6 |
| 构建 | Vite 8 |
| 样式 | styled-components 6（全部 CSS-in-JS，无独立 CSS 文件） |
| 路由 | react-router-dom 7（全部页面懒加载） |
| 动画 | CSS keyframes + React state + IntersectionObserver（无 GSAP / Framer Motion） |
| 地图 | d3-geo（经纬度投影）+ 手写 SVG + GeoJSON 管线 |
| 粒子 | seasonalfx（樱花花瓣系统） |
| 图标 | lucide-react |
| 包管理 | npm |

---

## 二、路由与页面导航

```
/login  ──→  Login（登录页 + 角色舞台）
/       ──→  Login（同上，默认路由）
/home   ──→  Home（首页门户 V2）
/jiangsu ──→ JiangsuMap（交互式地图，4 种视图模式）
/works  ──→  Works（占位页）
/about  ──→  About（占位页）
/blog   ──→  Blog（占位页）
/spirits ──→ SpiritDemo（角色演示页）
```

所有页面通过 `React.lazy` + `Suspense` 懒加载。App 顶层包裹 `ErrorBoundary` + `SettingsProvider`。

**页面间导航方式：**
- Login → Home：`nav("/home")`（模拟登录/游客）
- Home → JiangsuMap：`navigateWithBloom("/jiangsu?...")`（带光晕转场）
- JiangsuMap 内部：`setSearchParams()`（URL 参数驱动视图，无页面跳转）
- Works/About/Blog → Login：`nav("/")`（返回登录页）

---

## 三、页面详解

### 3.1 登录页（Login.tsx）

**文件规模：** ~45KB / ~1500 行

**布局：** 全屏背景图 + 居中 820×540 登录卡片，卡片分左右两栏。

| 区域 | 内容 |
|---|---|
| 左侧（400px）| 角色舞台：4 个小精灵（紫/黑/橙/黄）+ 品牌标识 + 页脚链接 |
| 右侧 | 登录表单：邮箱/密码输入、显示切换、记住我、登录按钮、游客入口 |

**4 个小精灵系统：**

| 精灵 | 颜色 | 形状 | 绑定能力 | 尺寸（缩放后） |
|---|---|---|---|---|
| 紫色 | 紫 | 180×400 矩形 | 地图探索 | 最大 |
| 黑色 | 深色 | 120×310 矩形 | 高校档案 | 中等 |
| 橙色 | 橙 | 240×200 半圆 | 经验分享 | 宽扁 |
| 黄色 | 黄 | 140×230 圆角 | 生活指北 | 中等 |

每个精灵有独立的眼睛跟随系统（PupilComp / EyeBallComp），鼠标在舞台上移动时眼球跟踪光标。点击精灵切换左侧技能说明卡。紫色精灵在密码可见时会偷看输入框。

**动效系统（6 组）：**
- `fadeUp` — 卡片入场（opacity 0→1 + translateY 16→0）
- `shakeAnim` — 登录失败抖动（translateX ±4px）
- `popIn` — 按钮弹跳（scale 1→1.08→1）
- `blinkAnim` — 精灵眨眼（scaleY 1→0.05→1）
- 角色呼吸 — 4 精灵各自正弦波动，频率各不相同
- 随机眨眼 — 紫色/黑色精灵 3-7 秒间隔随机眨眼 150ms

**共享视觉组件：** Bokeh、Komorebi、GroundPetals、DeskPet、Controls

---

### 3.2 首页 V2（Home.tsx）

**文件规模：** ~50KB / ~1900 行

**布局结构（从上到下）：**

```
┌──────────────────────────────────────┐
│  TopNav（品牌 + 导航胶囊）              │
├────────────────────┬─────────────────┤
│  HeroCopy（左侧）   │  PortalSide（右侧）│
│  · Eyebrow 标签    │  · GuidePanel    │
│  · Title 主标题    │    · SearchDock  │
│  · Subtitle 副标题 │    · 搜索建议     │
│  · ActionRow 按钮  │  · PrimaryPortalCard│
│  · CityChips 城市  │  · SubCardRow(3) │
│  · CityHint 提示   │  · StatsStrip(3) │
├────────────────────┴─────────────────┤
│  ScrollHint（向下滚动箭头）             │
├──────────────────────────────────────┤
│  SceneIndicator（圆点进度条）           │
│  Scene 1：从地图出发                   │
│  SceneConnector                       │
│  Scene 2：每所学校都是一本书（反转布局）   │
│  SceneConnector                       │
│  Scene 3：莉雅与你同行                 │
└──────────────────────────────────────┘
```

**背景层次（从下到上）：**

| z-index | 层 | 说明 |
|---|---|---|
| 0 | AnimatedBG | 背景图 + 28s 缓慢呼吸缩放 + 鼠标视差 |
| 1 | CloudShape ×3 | 38vw 模糊椭圆，48-62s 横跨漂移 |
| 1 | FXLayer | seasonalfx 樱花粒子 |
| 1 | Bokeh / Komorebi / GroundPetals | 散景光斑 / 光束 / 地面花瓣 |
| 2 | SoftWash | 三合一渐变遮罩（暗角+色调+晕影） |
| 3 | LargeDriftPetal ×4 | 80-140px 模糊花瓣，28-42s 漂移 |
| 8 | Shell / StoryRail | 主内容 + 叙事区 |
| 999 | DeskPet | 莉雅桌面宠物 |
| 2000 | RouteFlash | 路由光晕转场 |

**动效系统（10 组关键帧）：**

| 关键帧 | 用途 | 时长/周期 |
|---|---|---|
| `riseIn` | 卡片/文字分段入场 | 0.6-0.72s |
| `routeBloom` | 路由跳转光晕（scale 0.08→3.2） | 0.46s |
| `lightSweep` | 卡片扫光（translateX -130%→130%） | 7.2s 循环 |
| `primaryPulse` | 主卡片呼吸光晕 | 3.8s 循环 |
| `cardClickRipple` | 卡片点击波纹（scale 0.96→1 + 光圈扩散） | 0.44s |
| `bgBreath` | 背景缓慢缩放（scale 1→1.025→1） | 28s 循环 |
| `cloudDrift1/2` | 云状渐变横跨漂移 | 48-55s 循环 |
| `largePetalDrift` | 大花瓣漂移+旋转 | 28-42s 循环 |
| `scrollBounce` | 滚动提示箭头弹跳 | 2s 循环 |

**交互系统：**

| 交互 | 反馈 |
|---|---|
| 卡片 hover | 上浮 7px + 缩放 + 边框发光 + 扫光移动 + 图标旋转 + 箭头推进 |
| 卡片点击 | 波纹动画（440ms）→ 路由光晕（230ms触发）→ 跳转 |
| 搜索聚焦 | 边框变粉 + 背景增亮 + 阴影出现 |
| 搜索输入 | 实时建议下拉（城市+学校混合，最多6条） |
| 搜索键盘 | ↑↓ 选择 / Enter 确认 / Escape 关闭 |
| 城市标签 hover | 上浮 + 变粉 + 莉雅气泡切换城市推荐语 |
| 滚动 | 三场景依次入场（IntersectionObserver 触发） |
| 场景切换 | 圆点指示器随滚动自动切换 |
| 桌面宠物 | 可拖拽、双击切换表情、点击跳跃 |
| 移动端 | 右下角浮动菜单按钮（≤640px） |

**React 状态（12 个）：**

```typescript
search: string                    // 搜索输入
searchFocused: boolean            // 搜索框是否聚焦
activeCity: string | null         // 当前悬停的城市
guideMessage: string              // 莉雅引导文案
routeFlash: boolean               // 路由光晕是否显示
clickingCard: string | null       // 正在点击的卡片 ID
scrolledPastHero: boolean         // 是否已滚过首屏
reduceMotion: boolean             // 是否开启减少动效
activeSuggestionIndex: number     // 键盘选中的建议项
showMobileMenu: boolean           // 移动端菜单是否展开
// + 4 个 useInView 返回值（scene1-3, stats）
```

---

### 3.3 地图页（JiangsuMap.tsx）

**文件规模：** ~47KB / ~1760 行

**4 种视图模式：**

| 模式 | 触发条件 | 画面 |
|---|---|---|
| `province` | 无参数 | 江苏 13 城市标签 + 搜索面板 + 层级图例 |
| `city` | `?city=南京` | 城市大标题 + 高校列表 + 交互标记 + 统计卡片 |
| `school-preview` | `?city=南京&school=nju` | 浮动预览卡（层级/标签/图片/操作） |
| `school-detail` | `?city=南京&school=nju&view=detail` | 全页详情（英雄图/元数据/校园文化/院系/风景） |

**URL 驱动：** 所有视图状态通过 `useSearchParams()` 管理，可深链、可分享、可刷新。内部导航用 `setSearchParams()` 无页面跳转。

**SVG 地图层（MapCanvas）：**
- 书本页面背景（弧形纸张 + 阴影）
- 省级板块（5 层错位叠加 3D 纸质感）
- 城市路径（侧面/阴影/正面/高光/折痕 5 层 3D 变换）
- 高校标记（光束线 + 光晕椭圆 + 图钉 + 核心形状 + 标签）
- 每个层级不同形状：985=星形、211=菱形、双一流=星环、本科=双圆

**地图交互：** 拖拽平移 + 惯性衰减（惯性系数 0.9）、滚轮缩放（指数因子）、双击重置、点击城市聚焦放大。

---

### 3.4 占位页面（Works / About / Blog）

三个页面结构完全相同，仅文案不同。全屏背景图 + 深色遮罩 + 居中标题/提示/返回按钮。返回按钮导航到 `/`（登录页）。

---

## 四、共享组件详情

### 4.1 DeskPet（桌面宠物莉雅）

**文件：** `src/DeskPet.tsx`

| 属性 | 值 |
|---|---|
| 素材 | 4 张 PNG（calm / archery / happy / dance），384→110px 渲染 |
| 默认位置 | 右下角（window-170, window-180） |
| 拖拽 | Pointer Capture 实现，拖拽时无过渡动画 |
| 点击 | 跳跃 + 随机气泡（10 条消息池）+ 随机 emoji（✨💚🌸⭐🍃💫） |
| 双击 | 循环切换 4 张图片 |
| 空闲 | 3.5s 正弦浮动（-8px 峰值）+ 每 15s 20% 概率自言自语 |
| 鼠标跟随 | 80ms 去抖 + 0.04 线性插值，首页禁用（followMouse=false） |
| 气泡 | 双模式：引导气泡（宽 310px，前缀"莉雅："）+ 闲聊气泡（宽 240px，绿色文字） |
| z-index | 999 |

### 4.2 背景效果组件

| 组件 | 文件 | 说明 | 可配置项 |
|---|---|---|---|
| Bokeh | `Bokeh.tsx` | 浮动彩色光斑（圆形+模糊） | 数量/大小/透明度 |
| Komorebi | `Komorebi.tsx` | 光束（4 条斜角光柱）+ 尘埃粒子 | 光束透明度/尘埃数量 |
| GroundPetals | `GroundPetals.tsx` | 地面花瓣（小尺寸，柔和摇摆） | 数量 |
| seasonalfx | 外部库 | 樱花花瓣粒子系统 | 大小/速度/密度 |

所有背景效果组件使用 `stableRandom` 工具函数保证每次渲染位置一致（基于种子 + 盐值的确定性伪随机）。

### 4.3 Controls（设置面板）

**文件：** `src/Controls.tsx` | 位置：右下角固定 | z-index: 100

可实时调节 11 个视觉参数：视差强度、暗角深度、散景（数量/大小/透明度）、光束（透明度/尘埃数）、花瓣（大小/速度/密度/地面数量）。一键重置默认值。

### 4.4 JiangsuDataMapPreview（SVG 数据验证面板）

**文件：** `src/components/JiangsuDataMapPreview.tsx`

首页 StoryRail 场景 1 右下角的小型 SVG 地图预览。直接读取 `jiangsu-paths.json` 渲染 13 城市 path。城市可 hover（上浮+粉色描边）可点击（跳转地图）。采用懒加载拆包。

### 4.5 JiangsuSvgMap 子组件

**MapCanvas.tsx** — 完整 3D 纸叠地图渲染器（8 个滤镜 + 10 个渐变 + 多层架构）  
**InkBackground.tsx** — 水墨风格背景（极光渐变 + 透视网格 + 飘落花瓣 + 闪烁星点）  
**tokens.ts** — 色彩令牌（层级颜色 + 城市填充调色板）

---

## 五、数据层

### 5.1 高校数据（jiangsu-universities.ts）

```typescript
type Tier = "985" | "211" | "dual" | "provincial";

interface University {
  id: string;       // "nju", "seu" 等
  name: string;     // "南京大学" 等
  city: string;     // "南京" 等（不带"市"）
  lat: number;
  lng: number;
  tier: Tier;
  founded?: number; // 建校年份
  website?: string;
}
```

- **总数：** 78 所本科高校
- **城市：** 13 个地级市
- **层级分布：** 985（2 所）、211（9 所）、双一流（5 所）、本科（62 所）

### 5.2 地图数据管线

```
public/jiangsu-geo.json（原始 GeoJSON）
        │
        ▼
scripts/build-jiangsu-svg.mjs（d3-geo Mercator 投影）
        │
        ▼
src/assets/map/jiangsu-paths.json（13 城市 SVG path + 质心坐标）
```

### 5.3 工具函数

| 文件 | 主要导出 |
|---|---|
| `jiangsuPresentation.ts` | `cityKey`, `cityRouteParam`, `buildSchoolExhibit` |
| `jiangsuMap.ts` | `buildJiangsuMapData`, `findProjectedUniversity`（投影高校点位，金角螺旋避让） |
| `stableRandom.ts` | 确定性伪随机，用于视觉组件位置稳定 |

---

## 六、设置系统

```typescript
interface Settings {
  parallaxStrength: number;     // 0.5（鼠标视差强度）
  vignetteOpacity: number;      // 0.45（暗角深度）
  bokehCount: number;           // 20（散景数量）
  bokehSize: number;            // 1（散景大小倍率）
  bokehOpacity: number;         // 0.4（散景透明度）
  komorebiBeamOpacity: number;  // 0.25（光束透明度）
  komorebiDustCount: number;    // 40（光束尘埃数）
  petalSize: number;            // 1.8（花瓣大小倍率）
  petalSpeed: number;           // 0.6（花瓣速度倍率）
  petalIntensity: "minimal" | "subtle" | "moderate" | "bold";  // "moderate"
  groundPetalsCount: number;    // 25（地面花瓣数）
}
```

通过 React Context 全局共享，`SettingsProvider` 使用 `useState` 管理，无 localStorage 持久化。Controls 面板提供实时滑块调节。

---

## 七、自定义 Hooks

### useInView
```typescript
function useInView<T extends HTMLElement>(options?: {
  threshold?: number;      // 默认 0.15
  rootMargin?: string;     // 默认 "0px 0px -40px 0px"
  triggerOnce?: boolean;   // 默认 true（首触后断开 observer）
}): { ref: RefObject<T>; inView: boolean }
```
用于滚动触发入场动画，首页 4 个场景各一个实例。

### useMapTransform
```typescript
function useMapTransform(options?: {
  minScale?: number;  // 0.72
  maxScale?: number;  // 3.1
}): {
  svgRef, transform, reset,
  focusPoint, focusFeature,
  bind: { onPointerDown/Move/Up/Wheel/... }
}
```
地图页的拖拽平移 + 滚轮缩放 + 惯性衰减 + 平滑聚焦动画。

---

## 八、响应式断点速查

| 断点 | 影响范围 |
|---|---|
| 1100px | 地图统计卡片隐藏 |
| 1040px | 首页 HeroLayout 变单列 |
| 980px | StoryScene 变单列 |
| 960px | 地图浮动面板全宽、省份标签隐藏 |
| 900px | Shell 内边距缩减、SubCardRow 变 2 列 |
| 780px | 导航胶囊隐藏 |
| 760px | 数据面板全宽、场景徽章隐藏 |
| 640px | SubCardRow 变 1 列、StatsStrip 变 1 列、移动端 FAB 菜单出现 |

---

## 九、Z-Index 层级速查

| 层级 | 元素 |
|---|---|
| 0 | AnimatedBG（首页背景呼吸层） |
| 1 | FXLayer / CloudShape / Bokeh / Komorebi / GroundPetals / StageImage |
| 2 | SoftWash / VignetteOverlay / StoryCopy |
| 3 | LargeDriftPetal / SearchSuggestions |
| 4 | StageCaption / StoryBadges |
| 5 | DataProofDock |
| 7 | MarkerLayer（地图页） |
| 8 | Shell / StoryRail / ProvinceLabels |
| 9 | FloatingPanel / DetailShell（地图页） |
| 10 | TopBar / Breadcrumb / PreviewCard / BottomLegend |
| 99 | MobileMenuItem |
| 100 | Controls 面板 / MobileNavFab |
| 999 | DeskPet（莉雅） |
| 2000 | RouteFlash（路由光晕） |

---

## 十、色彩系统

| 用途 | 色值 |
|---|---|
| **主强调色** | `#ff4f83` / `#ff5d96`（粉/蔷薇） |
| **辅助蓝** | `#3a9cff` / `#69d6f4` |
| **深色文字** | `#101848` / `#111946` / `#151f4d` |
| **玻璃面板** | `rgba(255,255,255,0.5-0.9)` + `backdrop-filter: blur(14-24px)` |
| **暗色控件** | `oklch(0.18 0.005 0 / 0.55)` |
| **985 层级** | `#ff4f83`（粉） |
| **211 层级** | `#8b61ff`（紫） |
| **双一流** | `#3a9cff`（蓝） |
| **本科** | `#36c783`（绿） |

---

## 十一、构建与脚本

```bash
npm run dev          # Vite 开发服务器
npm run build        # tsc 类型检查 + Vite 生产构建
npm run build:map    # GeoJSON → SVG path 数据管线
npm run lint         # ESLint
npm run preview      # Vite 预览生产构建
```

**构建产物：** Home 49.8KB (gzip 13.1KB)、JiangsuMap 35.4KB (gzip 9.9KB)、总 vendor 263.6KB (gzip 86.6KB)

---

## 十二、当前已知问题与待改进

1. **登录→首页转场缺失** — 设计文档要求多步骤动画序列（小精灵聚拢→粒子→江苏轮廓→首页内容浮入），当前是直接路由跳转
2. **Works/About/Blog 返回按钮** — 导航到 `/`（登录页）而非 `/home`，应为后者
3. **莉雅状态不完整** — 设计文档要求的 5 个状态（idle/talking/guide/happy/sleep）当前只实现了 idle+跳+引导气泡
4. **地图页无 Controls** — Controls 面板只在 Login 和 Home 渲染，地图页无此入口
5. **无移动端导航** — Home 页 ≤640px 有 FAB，但地图页无移动端适配导航
6. **无加载骨架屏** — StageImageLayer 等图片无 onLoad 过渡
7. **搜索无"无结果"状态** — 输入无匹配内容时建议列表直接为空
8. **地图页 SVG 性能** — 78 个高校标记全部渲染，无虚拟化
